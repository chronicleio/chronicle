apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: lyra
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: lyra-unit
        static_configs:
          - targets:
              - lyra-unit-0.lyra-unit-headless.lyra.svc.cluster.local:7071
              - lyra-unit-1.lyra-unit-headless.lyra.svc.cluster.local:7071
              - lyra-unit-2.lyra-unit-headless.lyra.svc.cluster.local:7071
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: lyra
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      containers:
        - name: prometheus
          image: prom/prometheus:v3.2.1
          args:
            - --config.file=/etc/prometheus/prometheus.yml
            - --storage.tsdb.retention.time=7d
          ports:
            - containerPort: 9090
          volumeMounts:
            - name: config
              mountPath: /etc/prometheus
      volumes:
        - name: config
          configMap:
            name: prometheus-config
---
apiVersion: v1
kind: Service
metadata:
  name: prometheus
  namespace: lyra
spec:
  selector:
    app: prometheus
  ports:
    - port: 9090
      targetPort: 9090
