unit:
  replicaCount: 3
  image:
    repository: lyra-unit
    tag: dev
    pullPolicy: IfNotPresent
  server:
    bindAddress: "0.0.0.0:7070"
  ioMode: basic
  storage:
    size: 2Gi
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1"

xunit:
  enabled: true
  replicaCount: 1
  image:
    repository: lyra-xunit
    tag: dev
    pullPolicy: IfNotPresent
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"
    limits:
      memory: "1Gi"
      cpu: "1"

functions:
  enabled: true
  replicaCount: 1
  image:
    repository: lyra-functions
    tag: dev
    pullPolicy: IfNotPresent
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

orchestrator:
  enabled: true
  replicaCount: 1
  image:
    repository: lyra-orchestrator
    tag: dev
    pullPolicy: IfNotPresent
  server:
    bindAddress: "0.0.0.0:50051"
    endpoint: "http://lyra-orchestrator:50051"
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

catalog:
  backend: oxia
  serviceAddress: "oxia:6648"
  namespace: "default"

log:
  level: info
